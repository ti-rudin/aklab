# Типы недвижимости: apartment + land

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Добавить `land` (земельный участок) в enum, добавить `apartment` и `land` во все UI-селекты, вынести `classifyPropertyType` в `_shared` (DRY), обновить парсеры.

**Architecture:** `classifyPropertyType` дублируется во всех 10 парсерах с мелкими различиями. Выносим в `@aklab/service-shared`, каждый парсер заменяет локальную функцию на импорт. Параллельно обновляем enum в schema.json и все UI-селекты.

**Tech Stack:** Strapi 5 schema, Vue 3, TypeScript, 10 парсеров.

---

## Найденные проблемы

1. `apartment` уже есть в schema.json, но отсутствует в UI-селектах (фильтры, эталоны)
2. `land` отсутствует везде — падает в `other`
3. `classifyPropertyType` продублирован в 10 парсерах с расхождениями:
   - roseltorg: нет `apartment` (квартиры → other)
   - alfalot: `земельн` → free_purpose (должно быть land)
   - m-ets: apartment проверяется первым (OK, но отличается от остальных)
   - invest-mosreg, investmoscow: добавлен `апартамен`
   - Различия в keywords для free_purpose (`бокс`, `паркинг`, `здани`)
4. `isCommercialProperty` в `_shared/strapi-client.ts` фильтрует `земельн` как не-коммерческое (строка 79)

---

## Task 1: Schema — добавить `land` в enum

**Objective:** Добавить `land` в enum `property_type` в schema.json.

**Files:**
- Modify: `api/src/api/property/content-types/property/schema.json:119-126`

**Step 1:** Добавить `land` после `apartment`:

```json
"enum": [
  "office",
  "warehouse",
  "retail",
  "production",
  "free_purpose",
  "apartment",
  "land",
  "other"
],
```

**Step 2:** Commit.

---

## Task 2: formatters.ts — добавить labels

**Objective:** Добавить `apartment` и `land` в `typeLabel`.

**Files:**
- Modify: `app/src/utils/formatters.ts:9-17`

**Step 1:** Обновить typeLabel:

```ts
export const typeLabel = (v?: string): string =>
  ({
    office: 'Офис',
    warehouse: 'Склад',
    retail: 'Торговля',
    production: 'Производство',
    free_purpose: 'Свободного назначения',
    apartment: 'Квартира',
    land: 'Земельный участок',
    other: 'Другое',
  })[v || ''] || v || ''
```

**Step 2:** Commit.

---

## Task 3: UI — добавить apartment и land во все селекты

**Objective:** Обновить все дропдауны property_type в интерфейсе.

**Files (5 селектов в 4 файлах):**
- `app/src/views/PropertyListView.vue:182-189` — фильтр «Все объекты»
- `app/src/views/PropertyListView.vue:416-423` — фильтр «В фокусе»
- `app/src/views/MarketReferencesView.vue:29-36` — форма эталона
- `app/src/components/settings/MarketReferencesPanel.vue:22-29` — панель эталонов в настройках
- `app/src/views/SettingsView.vue` — если есть селект (проверить)

**Step 1:** В каждый селект добавить два option перед `other`:

```html
<option value="apartment">Квартира</option>
<option value="land">Зем. участок</option>
```

**Step 2:** Commit.

---

## Task 4: _shared — вынести classifyPropertyType

**Objective:** Создать единую функцию в `@aklab/service-shared`, убрав дублирование.

**Files:**
- Create: `services/_shared/src/property-classifier.ts`
- Modify: `services/_shared/src/index.ts` (добавить экспорт)

**Step 1:** Создать `services/_shared/src/property-classifier.ts`:

```ts
/**
 * Классификация типа недвижимости по тексту (название + описание).
 * Используется всеми парсерами для единообразной классификации.
 */
export function classifyPropertyType(text: string): string {
  const lower = text.toLowerCase();

  // Квартиры (высший приоритет — раньше было в free_purpose)
  if (lower.includes('квартир') || lower.includes('апартамен')) return 'apartment';

  // Земельные участки
  if (lower.includes('земельн') || lower.includes('участок') || lower.includes('земля')) return 'land';

  // Офисы
  if (lower.includes('офис') || lower.includes('административн')) return 'office';

  // Склады
  if (lower.includes('склад') || lower.includes('хранилищ')) return 'warehouse';

  // Торговля
  if (lower.includes('магазин') || lower.includes('торгов') || lower.includes('павильон')) return 'retail';

  // Производство
  if (lower.includes('производствен') || lower.includes('промышленн') || lower.includes('цех')) return 'production';

  // Свободного назначения (нежилые, здания, гаражи)
  if (lower.includes('нежилое') || lower.includes('помещение') || lower.includes('коммерческ') ||
      lower.includes('гараж') || lower.includes('бокс') || lower.includes('паркинг') ||
      lower.includes('здани')) return 'free_purpose';

  return 'other';
}
```

**Step 2:** Добавить экспорт в `services/_shared/src/index.ts`:

```ts
export { classifyPropertyType } from './property-classifier';
```

**Step 3:** Commit.

---

## Task 5: Парсеры — заменить локальный classifyPropertyType на импорт

**Objective:** Удалить дублированные функции из 10 парсеров, импортировать из shared.

**Files (10 парсеров):**
- `services/parser-fabrikant/src/sources/fabrikant.ts` — удалить строки 45-55, добавить импорт
- `services/parser-torgi-gov/src/sources/torgi-gov.ts` — удалить строки 21-30
- `services/parser-aggregator-bankrot/src/sources/aggregator-bankrot.ts` — удалить строки 33-43
- `services/parser-alfalot/src/sources/alfalot.ts` — удалить строки 17-27
- `services/parser-etprf/src/sources/etprf.ts` — удалить строки 17-27
- `services/parser-sberbank-ast/src/sources/sberbank-ast.ts` — удалить строки 17-27
- `services/parser-invest-mosreg/src/sources/invest-mosreg.ts` — удалить строки 27-37
- `services/parser-investmoscow/src/sources/investmoscow.ts` — удалить строки 31-41
- `services/parser-roseltorg/src/sources/roseltorg.ts` — удалить строки 12-21
- `services/parser-m-ets/src/sources/m-ets.ts` — удалить строки 34-44

**Step 1:** В каждом файле:
1. Добавить импорт: `import { classifyPropertyType } from '@aklab/service-shared';`
2. Удалить локальную `function classifyPropertyType(...)` 

**Step 2:** Commit.

---

## Task 6: strapi-client.ts — разрешить land в isCommercialProperty

**Objective:** Добавить `land` в COMMERCIAL_TYPES, убрать фильтрацию земельных участков.

**Files:**
- Modify: `services/_shared/src/strapi-client.ts:47,79-81`

**Step 1:** Добавить `'land'` в массив COMMERCIAL_TYPES (строка 47):

```ts
const COMMERCIAL_TYPES = ['office', 'warehouse', 'retail', 'production', 'free_purpose', 'apartment', 'land'];
```

**Step 2:** Убрать фильтр `земельн` из nonCommercialPatterns (строки 78-81):

```ts
  // Было:
  // if (title.includes('земельн') && !title.includes('нежилое')) {
  //   return false;
  // }
  // Ставим комментарий что земельные участки теперь разрешены (тип land)
```

**Step 3:** Commit.

---

## Task 7: Тесты — обновить под новую логику

**Objective:** Обновить тесты strapi-client и parse-handler.

**Files:**
- `services/_shared/__tests__/strapi-client.test.ts` — проверить что land не фильтруется
- `services/_shared/src/__tests__/` — добавить тест для classifyPropertyType из shared
- Проверить что существующие тесты парсеров не сломались

**Step 1:** Запустить тесты на dev-сервере: `cd ~/aklab && npx vitest run`

**Step 2:** Исправить упавшие тесты.

**Step 3:** Commit.

---

## Task 8: Build + Deploy

**Step 1:** Пушить, pull на dev, прогнать тесты.
**Step 2:** Коммитить, ждать команду на релиз.

---

## Verification

1. UI: на странице Объекты в фильтре «Тип» есть: Офис, Склад, Торговля, Производство, Св. назначения, Квартира, Зем. участок, Другое
2. UI: на странице Рыночные эталоны те же типы в форме создания
3. Парсеры: при парсинге титулов содержащих «земельный участок» → `property_type: 'land'`
4. Парсеры: при парсинге титулов содержащих «квартира» → `property_type: 'apartment'`
5. isCommercialProperty: земельные участки проходят фильтр
6. Все тесты зелёные
