HOST=0.0.0.0
PORT=1338
# Persistent private photo root shared by API reader and photo-fetcher writer.
# Must be an absolute path outside the release and web root.
PRIVATE_PHOTO_ROOT=/var/lib/aklab/private-photos
# Deprecated transitional alias; leave unset after migration.
# PHOTOS_BASE_DIR=/var/lib/aklab/private-photos
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified
