/**
 * market-reference service
 *
 * Фаза 1: stub. Фаза 6 (analyzer) будет читать активные эталоны через
 * findActive(city, propertyType) — добавится в Фазе 6.
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::market-reference.market-reference');
