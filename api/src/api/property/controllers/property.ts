/**
 * property controller
 *
 * Фаза 1: stub — стандартные CRUD-операции Strapi.
 * Фаза 5 добавит фильтры, populate, кастомные эндпоинты.
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::property.property');
